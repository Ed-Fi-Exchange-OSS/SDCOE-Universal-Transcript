import { formatDate } from 'utils/formatDate';

export const IMAGE_SIZE_LIMIT = 200; // size in KB
export const CHARACTER_LIMIT = 100;
export const BASE_YEAR = 1970; // base year for  attended school years
export const DATE_LIMIT = formatDate(new Date(), '-');

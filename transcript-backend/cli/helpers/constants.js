module.exports = {
  // Note that the resource dir configured in the config file
  // should take precedence
  DEFAULT_RESOURCE_DIR: './savedData',
  JSON_RESOURCE_DIR: 'json',
  PDF_RESOURCE_DIR: 'pdf',
  SECRET_RESOURCE_DIR: 'secret',
  CONFIG_TEMPLATE_PATH: './cli/configs/settings.example.json',
  CONFIG_FILE: 'settings.json',
  RSA_BIT_LENGTH: 2048
};

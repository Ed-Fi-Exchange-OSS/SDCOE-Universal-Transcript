/**
 * This function takes input as an object {value} and returns whether the object contains value or not
 * @param {Object}
 * @returns {boolean}
 */

export const isEmptyInputField = value => {
  if (!value) return true;
  return false;
};
